
import { TextTreeNode } from "@atomist/rug/tree/PathExpression"

import { NodeFilter } from "./NodeFilter"

/**
 * Sort the nodes in this tree. Node that it returns a new tree.
 * @param tn 
 * @param filter 
 */
export function sort(tn: TextTreeNode, filter: NodeFilter): TextTreeNode {
    return null;
}